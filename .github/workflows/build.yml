name: Python CI
'on':
  push:
    branches:
      - mert
  pull_request:
    branches:
      - sanem
      - mert
jobs:
  build:
    if: github.event_name == 'push'
    env:
      JOB_STATUS_UNIT: ok
      JOB_STATUS_SCA: ok
      GITHUB_TOKEN: '${{ secrets.GITHUB_TOKEN }}'

